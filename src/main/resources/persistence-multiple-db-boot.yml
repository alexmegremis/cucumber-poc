application:
  datasource:
    jdbcUrl: jdbc:h2:tcp://localhost:9101/cucumberPOC;FILE_LOCK=NO
    password: ''
    username: sa
batch:
  datasource:
    jdbcUrl: jdbc:h2:tcp://localhost:9101/cucumberPOCbatch;FILE_LOCK=NO
    password: ''
    username: SABATCH
hibernate:
  cache:
    use_query_cache: false
    use_second_level_cache: false
  dialect: org.hibernate.dialect.H2Dialect
  hbm2ddl:
    auto: validate
